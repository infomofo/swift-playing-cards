name: Lint

on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: SwiftLint + SwiftFormat
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install mise
        uses: jdx/mise-action@v4

      - name: Lint
        run: mise run lint
