name: Lint

on:
  pull_request:
    branches:
      - main

jobs:
  swiftlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install mise
        uses: jdx/mise-action@v4

      - name: SwiftLint
        run: mise exec -- swiftlint lint
